import { makeStyles } from '@mui/styles'

const useHotelList = makeStyles({
    container: {
        display: 'flex',
        justifyContent: 'center',
        flexWrap: 'wrap',
        marginBottom: 5,
    },
})

export default useHotelList
